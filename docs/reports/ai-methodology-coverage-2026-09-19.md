# AI 小说创作方法论 × NovelMuse 实现度比对

> 比对对象：视频《其实AI小说一点都不难做的喵》整理出的 **11 类 / 97 个方法论名词**
> 比对时间：2026-09-19 ｜ 依据：`apps/plugins/local/novel.autowrite/`、`apps/server/src/ai/`、`packages/db/`、`apps/web/src/` 的实际代码
> 判定口径：**✅ 已实现**（有独立机制/落库/UI，不依赖人肉）；**⚠️ 部分实现**（仅 prompt 级、或能力存在但未接线/不可调）；**❌ 未实现**（全仓无对应代码）

## 总账

| 判定 | 数量 | 占比 |
|---|---|---|
| ✅ 已实现 | **29** | 29.9% |
| ⚠️ 部分实现 | **19** | 19.6% |
| ❌ 未实现 | **49** | 50.5% |
| 合计 | 97 | 100% |

一句话结论：**这套方法论的前 2/3（AI 写作工程）覆盖度很高，后 1/3（数据驱动与商业运营）基本空白。**
项目做的是一个**质量导向的多智能体创作引擎**，不是一个**增长导向的网文工厂**。

## 分类覆盖率

| # | 类别 | 项数 | ✅ | ⚠️ | ❌ | 覆盖率 | 评价 |
|---|---|---|---|---|---|---|---|
| 一 | 选题与市场定位 | 4 | 1 | 1 | 2 | 50% | 弱 |
| 二 | 世界观与设定 | 5 | 2 | 2 | 1 | 80% | 良 |
| 三 | 角色系统 | 6 | 4 | 2 | 0 | **100%** | 优 |
| 四 | 大纲结构 | 8 | 4 | 2 | 2 | 75% | 良 |
| 五 | 生成参数与采样 | 8 | 2 | 1 | 5 | 38% | **差** |
| 六 | 上下文与记忆管理 | 8 | 4 | 3 | 1 | 88% | **优** |
| 七 | 叙事与文笔技巧 | 15 | 1 | 4 | 10 | 33% | **最差** |
| 八 | 去AI化与文本质量 | 8 | 1 | 1 | 6 | 25% | **差** |
| 九 | 多智能体创作架构 | 8 | 7 | 0 | 1 | **88%** | **最优** |
| 十 | 质检与合规 | 12 | 2 | 3 | 7 | 42% | 差 |
| 十一 | 数据驱动与商业运营 | 15 | 1 | 0 | 14 | 7% | **空白** |

---

## 一、选题与市场定位（1✅ / 1⚠️ / 2❌）

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 核心金手指 | ⚠️ | 无「金手指」字段。流派目录有「系统流」分组（`novelGenres.ts:32`），策划官要求「力量体系必须写代价与上限」（`pipeline/roles-phase.ts:80`），但**没有规则自洽的能力边界机制** |
| 核心脑洞 | ✅ | `projects.brief.opening/worldview` 由新书向导收集，注入为【创作设定】并明示「与设定库同等效力，不得违背」（`discuss/orchestrator.ts:281`）；`briefHash` 锁基线，改动即让已过闸门过期（`pipeline/types.ts:118`） |
| 一句话高概念卖点 | ❌ | 有 `projects.description` 但只是简介字段，**未作为 prompt 首行锚点**，无 logline 机制 |
| 读者画像与圈层定位 | ❌ | **全仓无 few-shot 范文注入**。`novel.bookscan` 抓晋江/番茄/七猫榜单入 `reference_books`，但**不喂给模型**（仅作阅读对照） |

## 二、世界观与设定（2✅ / 2⚠️ / 1❌）

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 细分题材标签矩阵 | ⚠️ | `novelGenres.ts` 两级目录：系统流 6 组（末日天灾/都市系统/修炼升级/穿越诸天/模拟经营/悬疑诡异）+ 无系统流 8 组，落 `projects.genre`。但**单选**，非多标签组合 |
| 感官具象化世界观提示词 | ⚠️ | 策划官要求「日常质感：吃什么、怎么通讯、钱怎么算、普通人怕什么」（`roles-phase.ts:83`）—— 属具象化，但无「至少三感官通道」约束 |
| 设定冲突自检护栏 | ✅ | 四重：设定管家查库（唯一必须查库角色，`roles.ts:120`）+ 校对门（全文 × 设定库，`gates.ts:112`）+ drift L3 层（`drift.ts:12`）+ `fact_conflicts` 冲突队列（同批次矛盾两条都不写，`ingest.ts:47`） |
| 角色口吻与声调提示词 | ✅ | `characters.speech_style` 字段（`project_tables.sql:36`）→ digest 注入「语言风格=…」（`context-resolver.ts:163`）+ 对白打磨师技能 |
| 记忆点行为锚 | ❌ | 无「标志性小动作重复强化」机制 |

## 三、角色系统（4✅ / 2⚠️ / 0❌）★ 覆盖最完整

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 角色详细档案卡 | ✅ | `characters` 表：`role/desire/fear/belief/weakness/personality/speech_style/backstory/aliases/relations/states`（`project_tables.sql:36`），每章经 digest 注入 |
| 角色成长弧光 | ✅ | cast 段「主角弧光」「节奏纲领：弧光转折点落在故事哪个阶段」（`roles-phase.ts:110`）+ `characters.states` 状态流水 |
| 主角核心动力与致命缺陷 | ✅ | `desire`（动力）+ `fear`/`weakness`（缺陷）独立字段，digest 注入「欲望=/恐惧=」（`context-resolver.ts:163`） |
| 反派具象再现原则 | ⚠️ | cast 段有「对手与压力源：谁在挡路，力量对比如何变化」，但**无独立反派档案 + 视角切换生成** |
| 人物羁绊与动态关系网 | ✅ | `characters.relations` JSON（`CharacterRelation{targetId,type,direction,chapter}`）+ `RelationGraph` 浮窗可视化 + 每章更新 |
| 横梗与功能型配角配置 | ⚠️ | cast 段「核心配角：欲望/恐惧/关系演变/退场点」，但**无 narrative function 标签化分工** |

## 四、大纲结构（4✅ / 2⚠️ / 2❌）

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 黄金前三章法则 | ✅ | `pilot` 段专写前三章（`PILOT_CHAPTERS=3`）+ `premiere-reviewer` 跨章审阅 + G4 闸门（`pipeline/pilot.ts:2`） |
| 章末端章钩子 | ⚠️ | prompt 级：剧情设计师「再给关键转折与结尾钩子」（`roles.ts:52`）；评审官维度含「章节钩子」（`prompts.ts:47`）。**无「末 200 token 设未解决冲突」的工程化控制** |
| 百万字级宏观大纲 | ✅ | `outline_nodes` 树状表（`parent_id` + type 幕/章/场景/节拍，`project_tables.sql:217`）+ plot 段剧情总纲 + `targetWordCount` |
| 分卷阶段大终极 | ⚠️ | outline 有「幕」层级，但**无每卷 checkpoint + consistency review**；drift 是全局分段核查 |
| 中期副本单元结构 | ❌ | 无「副本/单元」概念 |
| 单章细纲节拍卡 | ✅ | 定稿官契约首行即「节拍：3–4 个节拍，用 → 连接」（`roles.ts:186`）+ `outline_nodes` 含节拍类型 + `ChapterPlanGraphPanel` 可视化 |
| 草蛇灰线伏笔与回收闭环 | ✅ | `foreshadows` 表：`seed_chapter/payoff_chapter/status(planted\|hinted\|payed_off\|abandoned)/type(identity\|motivation\|relation\|trauma\|turning\|fate)/hints/earmarks`（`project_tables.sql:140`）+ 伏笔追踪者技能 + 伏笔管理页 + 定稿官契约「伏笔」栏 |
| 主线与支线配比平衡 | ❌ | 无章节标签 + 配比计数机制 |

## 五、生成参数与采样（2✅ / 1⚠️ / 5❌）★ 最薄的一环

统一入口 `apps/server/src/ai/providers/provider-factory.ts`，`ChatOptions` 只有 `temperature`/`maxTokens`/`tools`/`toolChoice`，实际发往 API 的 body 只有 `model/messages/temperature/max_tokens`。

| 名词 | 判定 | 代码事实 |
|---|---|---|
| temperature | ⚠️ | 各 agent 硬编码 0.1–0.8（`chat-agent.ts:708`=0.7、`consistency-agent.ts:125`=0.2、`timeline-analysis-agent.ts:131`=0.6…），**前端完全不可调**（`AIConfigPanel.tsx:36` 只暴露 baseUrl/apiKey/model） |
| top_p / top_k | ❌ | 全仓 0 命中 |
| presence_penalty | ❌ | 全仓 0 命中 |
| frequency_penalty | ❌ | 全仓 0 命中 |
| 上下文窗口 | ✅ | 预算控制完备：`WHEEL_MAX_CHARS=12000`、`PREV_CHAPTER_MAX_CHARS=6000`、`MAX_FACT_CHARS=6000`、`MAX_REF_BOOK_CHARS=4000`、`MAX_PREV_CHAPTER_CHARS=3000` |
| stop sequences | ❌ | 全仓源码 0 命中 |
| 深度思考 | ✅ | 最强项之一：`reasoning_content` 旁路实时推流（`thinking` 事件，`orchestrator.ts:60`）+ 思考 token 计入上限的踩坑修复 + 宿主层护栏 `isStarvationFailure`（`plugin/host.ts`） |
| 小样本典范 few-shot | ❌ | **无范文注入**。参考书仅截断塞入作对照（`context-builder.ts:180`），非风格模仿 |

## 六、上下文与记忆管理（4✅ / 3⚠️ / 1❌）★ 覆盖最完整

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 上下文漂移与遗忘症 | ✅ | 已识别并三重应对：水车 + 章节摘要 + 实体沉淀 |
| 滚动滑动窗口机制 | ✅ | **水车记忆**（`framework/memory/waterwheel.ts`）：`WHEEL_CAPACITY_DEFAULT=3` 斗、`WHEEL_MAX_CHARS=12000`、斗 1 无条件收、出界压一句话（`evictPrompt`）、压缩失败挂 `pending` 绝不静默丢弃；**每个 agent 各转各的水车**（`wheel-store.ts:3`） |
| 检索增强生成 RAG | ⚠️ | 有「按需回读」（`recall-by-need`，补写时回读斗位外更早章节，`orchestrator.ts:447`）+ 工具查库（`list_chapters`/`read_chapter`）+ digest 装配。**但无向量检索** |
| 设定集向量知识库 | ❌ | 无 embedding / 向量库（全仓无 embedding 相关代码） |
| 人物与事件关系知识图谱 | ⚠️ | 有 `RelationGraph` 可视化 + `knowledge/graph/` + `relations` JSON，但**底层是 JSON 不是图库，无图遍历检索** |
| 递归式章节摘要链 | ⚠️ | `chapters.summary` 字段 + `SUMMARY_SYSTEM`（120 字含钩子）+ 水车出界压一句话 —— 均为**单层**，无「摘要的摘要做卷摘要」 |
| 角色动态状态表 | ✅ | `characters.states` / `items.states` / `items.current_holders` 结构化 JSON，每章结束后更新（`entity-sink.ts`）；`WorldStateBoard`「最近变动」读三类（角色/物品/伏笔） |
| 绝对时间线与纪年校准 | ✅ | `timeline_events` 表 + `chapters.story_time`（注释明确「故事内时间≠章号」）+ `extractStoryTime` 从契约抽取 + **水车第三层时间戳** + 设定管家核对「时间线是否对得上」 |

## 七、叙事与文笔技巧（1✅ / 4⚠️ / 10❌）★ 最大缺口

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 白描动作替代法则 | ❌ | writer prompt 无「禁止直接写情绪词」约束 |
| 三重感官沉浸法 | ❌ | 无多感官通道约束 |
| 场景气感与氛围渲染 | ❌ | 无氛围关键词列表 |
| 场景摩擦力与受阻感 | ❌ | 无阻力点预设 |
| 台词潜台词与话里有话 | ⚠️ | 对白打磨师技能「关注对白中的潜台词与言外之意」（`dialogue-polisher.ts:17`）—— 仅 prompt 级 |
| 对话对抗张力 | ❌ | 无「每句推进/反击/试探」规则 |
| 权力地位升降博弈 | ❌ | 无 status score |
| 专属个性口语标识 | ✅ | `speech_style` 字段 + 对白打磨师「检查对白是否符合各角色说话风格与口头禅」+ 消除「所有人说话一个味」 |
| 心理活动与表象反差 | ❌ | 无双轨生成 |
| 欲扬先抑与压抑蓄力 | ⚠️ | cast 段「张与弛的呼吸节奏」「高潮前的蓄势」（`roles-phase.ts:112`） |
| 爽感宣泄与暴力破局 | ❌ | 无 |
| 经典打脸四部闭环 | ❌ | 无四 beat 模板 |
| 绝对信息差爽感 | ❌ | 无「读者视角 vs 角色视角」信息披露控制 |
| 战力通胀防洪堤 | ⚠️ | 策划官「力量体系必须写代价与上限」+ drift L2/L3 核查冲突。**无战力数值表/天花板** |
| 战利品盘点与多巴胺结算 | ⚠️ | `items.credit_price` + `credit_transactions` 积分流水表（系统文结算）；但**无战斗后「收获清单」生成** |

## 八、去AI化与文本质量（1✅ / 1⚠️ / 6❌）

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 去AI化文本重构 | ❌ | 无 `humaniz` 相关代码 |
| 困惑度与突发度调优 | ❌ | 无 perplexity / burstiness 计算 |
| 细化词具象化替换 | ❌ | 无反向词典 |
| 对话系数黄金配比 | ❌ | 无对话行占比统计 |
| 手机瀑布流排版优化 | ⚠️ | `typography` 插件（字重 300–700/颜色）+ `ExportDialog`（md/html/txt/pdf/docx/epub）。**无「每段 2–4 句」规则** |
| 前后因果自检修剪 | ✅ | 校对门（`runConsistencyGate`）+ `consistency-agent` 全文 × 设定库比对，冲突清单 + 定向修订指令 |
| 同音字与生僻乱码清洗 | ❌ | 无 |
| 修辞过度意境 | ❌ | 无修辞密度上限（润色门评分会评文笔，但不针对辞藻堆砌） |

## 九、多智能体创作架构（7✅ / 0⚠️ / 1❌）★ 覆盖最优

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 多智能体协同创作架构 | ✅ | 8 个角色（剧情设计师/角色设计师/设定管家/定稿官/写作官/意图复核/策划官/前三章审阅）+ 共享 `transcript` + **记忆隔离**（`agent_memory` 按 `agent_id` 分区） |
| 架构大纲智能体 | ✅ | plot 段（`roles-phase.ts`）+ 大纲架构师技能（`outline-architect.ts`） |
| 扩写正文智能体 | ✅ | 写作官 `ROLE_WRITER`（吃定稿官契约）+ 智能续写技能 |
| 质检跳词智能体 | ✅ | 意图复核 + 校对官 + 评审官 + 节奏诊断师 + `scanner-agent` |
| 设定对齐智能体 | ✅ | 设定管家 + 校对门 + drift 三层核查 |
| 提示词生成流水线 | ✅ | `context-resolver` digest 装配 + 记忆闸门裁剪（`digest-gate.ts`）+ 技能注入（`agent-skills.ts`）+ 契约模板填充 |
| 多章并行推演管线 | ❌ | **明确不做**：连写是**逐章串行**（`runChapters`，`orchestrator.ts:790`），无同章多版本打分选优、无 `n`/seed |
| 人机协同关键把关 | ✅ | 4 道闸门 G1–G4 + 三态决策（approve/revise/reject）+ 单章三道门（意图/校对/润色） |

## 十、质检与合规（2✅ / 3⚠️ / 7❌）

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 原创度安全护栏 | ❌ | 无 n-gram / SimHash 比对 |
| AI抄袭融梗查重 | ❌ | 无 |
| 平台敏感词与合规审查 | ❌ | **全仓 grep `敏感词`/`违规`/`compliance` = 0 命中** |
| 平台AI创作准入与申报规范 | ❌ | 无 |
| AI创作著作权切权 | ⚠️ | `memory_audit` 表记录完整装配指纹（`fingerprintOf`，`gate.ts:74`）+ `ai_conversations` —— 可作人类贡献证据链，但**无版权声明机制** |
| 现实映射与名誉侵权红线 | ❌ | 无 NER 与现实实体库比对 |
| 角色人设不崩坏质检 | ✅ | 设定管家 + 校对门（全文×设定库）+ 角色档案比对 + 14 例单测守护（`entity-sink.test.ts`） |
| 逻辑漏洞与战力崩坏率 | ✅ | drift 逐条断言式反事实核查（L1 brief / L2 已批契约 / L3 项目库事实，`drift.ts:11`）+ `driftCounts` 可判定统计 |
| 速度流畅度指数 | ❌ | 无 Flesch 类指标 |
| 读者弃书断点预测 | ❌ | 无 |
| 爽点兑现有效度 | ⚠️ | 意图门对照「本章结论」验收（节拍/关键物/角色变化是否落实），但**无「爽点」这个概念** |
| 水文注水膨胀率监测 | ⚠️ | 有字数契约（`WRITER_MIN_CHARS=2500` + 低于下限补写一次）+ `proseLength` 去空白计数；**无「每千字新信息量」检测** |

## 十一、数据驱动与商业运营（1✅ / 0⚠️ / 14❌）★ 整块空白

| 名词 | 判定 | 代码事实 |
|---|---|---|
| 次日追读率 | ❌ | 无埋点、无留存统计 |
| 上架首订与付费转化率 | ❌ | 无 |
| 章节留存与付费跳失曲线 | ❌ | 无 |
| 书名与简介点击率测试 | ❌ | 无 A/B |
| 算法推荐跃迁机制 | ❌ | 无 |
| 双版本正文竞技择优 | ❌ | 无灰度发布 |
| AI多播有声小说 | ❌ | 无 TTS（且产品不支持多模态生成） |
| 爆款短剧剧本逆向提取 | ❌ | 无 |
| AI漫剧与视觉资产联动 | ❌ | 无（仅书封生成） |
| AI网文出海多语言本地化 | ❌ | 无翻译管线 |
| 读者粉丝社群共创飞轮 | ❌ | 无 |
| Token投入产出收益比 | ❌ | **完全无 token/费用统计**（全仓无 `usage`/`prompt_tokens` 记录） |
| 长尾完本书阵列收益 | ❌ | 无 portfolio 管理 |
| 提示词资产化与流程复用 | ✅ | `skill_library` 表 + `agent_skill_toggles`（`0001_skills_library.sql`）+ 技能库面板 + `prompts.ts` 模板集中管理 |
| 读者数据反馈驱动动态演进 | ❌ | 无数据回流闭环 |

---

## 实现了、但清单里没写的（超出方法论的部分）

这几项是本项目**独有的工程贡献**，方法论文档里没有对应名词：

| 能力 | 位置 | 为什么有价值 |
|---|---|---|
| **记忆权限闸门**（fail-closed） | `framework/memory/gate.ts` | 角色 × 键族白名单 × 具名 reason（枚举里没有「想看看」）；**缺省空集 = 读不到**；越权写被类型与运行期双重拦截 |
| **装配指纹** | `gate.ts:74` `fingerprintOf` | 「同输入是否同结果」的可执行形式（hash + 字数），回答「它当时看到的是哪一份」 |
| **事实冲突队列** | `ingest.ts:47` `fact_conflicts` | 同批次同槽位两个值 = 模型自相矛盾 → **两条都不写**，进队列等人裁；明确拒绝「新值自动覆盖旧值」 |
| **出界不丢** | `waterwheel.ts` | 水车挤出的斗：能压就压成一句话，压不了把原文挂 `pending`，绝不静默丢弃 |
| **意图门只判硬约束** | `roles.ts:238` | 实测教训换来的：把「同一件事的表演方式」排除出打回理由（曾因「袖口扣子主动解还是被蹭开」连打两次回，最终 3333 字全废） |
| **带警示交付** | `orchestrator.ts:700` | 门失败不丢章，`delivered.warnings` 标出来（30 章连写曾因「不通过不交付」丢了 2/7 章） |
| **门失败必须可落地** | `gates.ts:22` | 「以后新增任何门，先问一句：它失败时该怎么落地？」—— 静默 `catch` 让门成摆设是踩过的坑 |

## 关键判断

1. **项目的定位不是「网文流水线」**。README 明确写着「与生成内容的 AI 写作助手不同，NovelMuse 通过追踪所有叙事元素，帮助作家保持故事的一致性」—— 方法论后半段（留存/付费/A-B/有声/短剧）属于**平台方**的活，不在本工具的射程内。这 14 项空白**不构成缺陷**，但**是产品边界**。

2. **真正的短板在「生成参数」与「去AI化」**（五、八类，合计 16 项只有 3 项完整）。这两块是**方法论里唯一"纯工具侧"却做薄了的部分**：
   - `temperature` 硬编码在代码里，作者无法按章节调（对话章节该高、设定章节该低）
   - 无 `top_p`/`frequency_penalty` —— 而「车轱辘话」「AI 味」正是这两项的靶子
   - 无 few-shot 范文 —— 「读者画像与圈层定位」「风格对齐」因此都没有抓手
   - 无去AI化后处理 —— 本项目靠**多角色门禁**保证质量，而非**后处理**，路线不同但代价是「AI 味」无处治理

3. **叙事技巧（七类 1/15）几乎全空**，但其中大半（爽点/打脸/信息差）是**题材套路**而非通用写作能力。项目选择了「让角色讨论出结论」而不是「套模板」，这条路线本身自洽 —— 只是意味着**它写不出爆款网文的爽感节奏**，写的是「讲得通的故事」。

4. **最强的三块**（九 多智能体 7/8、三 角色系统 4/6、六 记忆管理 4/8）恰好是**工程难度最高**的三块。这说明投入方向是对的：难的地方做扎实了，容易的地方（调参数、加后处理）随时能补。

## 如果要补，优先级建议

| 优先级 | 事项 | 成本 | 收益 |
|---|---|---|---|
| P0 | 暴露 `temperature`/`top_p`/`frequency_penalty` 到 AI 设置面板 | 低（`provider-factory.ts` 加字段 + UI 加控件） | 直接治「AI 味」与重复用词 |
| P1 | 参考书范文 few-shot 注入（把 `reference_books` 已抓的样本真正喂进 prompt） | 中 | 补上「风格对齐/读者定位」的抓手，数据已存在只是没接线 |
| P1 | 去AI化后处理门（困惑度/句长方差/对话占比） | 中 | 与现有三道门互补，不依赖模型自觉 |
| P2 | Token 消耗统计（`usage` 落库） | 低 | 长跑场景的成本可见性，现在完全黑盒 |
| P2 | 敏感词/合规扫描（发布前） | 中 | 十类里唯一有「法律风险」性质的缺口 |
| P3 | 同章多版本打分择优（`n` + 评分模型） | 高（调用量 ×3） | 质量上限提升，但成本敏感 |
